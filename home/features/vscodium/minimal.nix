{
  programs.vscode.userSettings = {
    window = {
      menuBarVisibility = "hidden";
    };

    workbench = {
      activityBar.visible = false;
    };

    editor = {
      minimap.enabled = false;
      scrollbar = {
        horizontal = "hidden";
        vertical = "hidden";
      };
    };
  };
}
